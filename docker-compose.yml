version: '3.8'

services:
  app:
    build: .
    container_name: crm-app
    restart: always
    ports:
      - "3003:3000"
    environment:
      - NODE_ENV=production
      # - DATABASE_URL=postgres://user:password@db:5432/mydb
      # - GEMINI_API_KEY=your_gemini_api_key
      # - JWT_SECRET=your_jwt_secret
    # If you want to use a postgres container instead of an external DB, you can uncomment below:
    # depends_on:
    #   - db

  # db:
  #   image: postgres:15-alpine
  #   container_name: crm-db
  #   restart: always
  #   environment:
  #     - POSTGRES_USER=user
  #     - POSTGRES_PASSWORD=password
  #     - POSTGRES_DB=mydb
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

# volumes:
#   pgdata:
